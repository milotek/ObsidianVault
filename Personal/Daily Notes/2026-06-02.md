- `syncMocks()` reads mocks from `chrome.storage.local` in `WebPageCommunicationService` and pushes them via `evaluateScript` 
	- This method is called in `panel.ts` once when we initialise the panel...
		- For this, I've added a `initPanel()` function which wraps both the new `syncMocks()` and the previous `fetchAndDisplayContent` together.
	- ...and once more when in `handleAddMock`, which closes the todo comment from a previous CL.
- Instantiates `ClientEmulator` in `SilkWebInspector` to receive and store them. In the future we should only create if Client Emulator setting in WebDevX preferences is enabled.
- Triggers sync on panel init and whenever a mock is saved.