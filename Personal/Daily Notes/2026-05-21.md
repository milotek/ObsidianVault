```ts
if (emulator.shouldMock(serviceName, methodName)) {
	const mock = emulator.getMockMethod(serviceName, methodName);
}
else {
	// ... post original msg
}
```

```ts
const mock = emulator.getMockMethod(serviceName, methodName);
if (mock) {
	// ... post mock
} 
else {
	// ... post original msg
}
```

