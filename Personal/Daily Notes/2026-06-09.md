```
>be protoshop devs
>add feature to pass in base64 proto data in URL
>use standard b64 decoder and not url safe base64 encoder specifically for this feature despite the fact you have web safe decoding as an option
>don't provide param to change decoder type
>literally hardcode none url safe base64 decoder for tool specifically built to be used with urls
>tfw
```

![[Pasted image 20260609104728.png|300]]

---
[[Tiny silk android base64 newline bug]]