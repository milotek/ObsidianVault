## Tasks
- [ ] Finish SIlk Client emulation
- [ ] Practice neetcode
- [ ] Write snippets

## Prompts

> [!NOTE]
> Create the UnifiedPortsTransportSimulator for client emulation.
> 
> Create a new file @google3/search/frontend/silk/tools/webinspector/injected/unified_ports_transport_simulator.ts:
> 1.  Define and export a class `UnifiedPortsTransportSimulator`.
> 2.  The constructor should accept the `window` object.
> 3.  Implement private methods to simulate native-to-web communication for method responses and events. These methods should be flexible and driven by internal state rather than hardcoded data.
> 4.  Add public methods to:
>     *   `startSimulation()`: Sets up the message channel and attaches listeners. Exposes the web port on the window object (e.g., as `silkPort`).
>     *   `stopSimulation()`: Cleans up listeners and removes the port from the window.
>     *   `updateMockMethodResponses(mocks: MockMethodResponses)`: Updates the internal state for mock method responses.
>     *   `triggerEvent(eventDetails: EventDetails)`: Posts a message to the web port to simulate a native event.
>     *   `setEnabled(enabled: boolean)`: Controls whether the simulator is actively intercepting and responding to messages.
> 5.  Initialize with an empty set of mock responses.
> 
> Define helper interfaces for `MockMethodResponses` and `EventDetails` in @google3/search/frontend/silk/tools/webinspector/common/types.ts.
> 
> Add the new `ts_library` to @google3/search/frontend/silk/tools/webinspector/injected/BUILD.

> [!NOTE]
> Add a Client Emulator section to the Silk Web Inspector UI.
> 
> Edit @google3/search/frontend/silk/tools/webinspector/app/panel.html:
> 1.  Below the existing "feature list" divs (e.g., "services", "methods", "events"), add a new collapsible section similar to the others:
> 2.  Inside `client_emulator_content`:
>     *   Add a toggle switch or checkbox to "Enable Client Emulation".
>     *   Add subsections for "Mock Method Responses" and "Mock Event Triggers".
>     *   In "Mock Method Responses": Input fields for Service Name, Method Name, Response Type (`method_result` or `method_error`), JSON Response Data (textarea), and an "Add Mock" button. Also, a table/list to display added mocks with a "Delete" button.
>     *   In "Mock Event Triggers": Input fields for Service Name, Event Name, JSON Event Data (textarea), and a "Trigger Event" button.
> 
> Edit @google3/search/frontend/silk/tools/webinspector/app/panel.ts:
> 3.  Add event listeners for the new "Client Emulator" expand/collapse header.
> 4.  Manage the state of the emulator (enabled, mock definitions).
> 5.  Handle UI interactions: adding/deleting mock methods, triggering events.
> 6.  (Placeholder for now) Prepare functions to communicate these actions to the injected script.

edit:
Implement the "Enable Client Emulation" toggle and state.

Edit @google3/search/frontend/silk/tools/webinspector/app/panel.html:
1.  Inside the `emulator_controls` div, add a toggle switch or checkbox:

Edit @google3/search/frontend/silk/tools/webinspector/app/panel.ts:
1.  Add a boolean variable to manage the enabled state of the client emulator.
2.  Add an event listener for the `enable_client_emulation` checkbox to update the state variable.
3.  (Placeholder): This state change will later be communicated to the injected script.




> [!NOTE]
> Connect the Client Emulator UI to the injected simulation logic.
> 
> Edit @google3/search/frontend/silk/tools/webinspector/app/panel.ts:
> 1.  Use `WebPageCommunicationService` to send messages to the injected script when UI actions occur:
>     *   When "Enable Client Emulation" is toggled.
>     *   When mock method definitions are added or removed.
>     *   When an event is triggered.
> 
> Edit @google3/search/frontend/silk/tools/webinspector/common/web_page_communication_service.ts:
> 2.  Add methods to call functions on `window.silkWebInspector` via `chrome.devtools.inspectedWindow.eval`. Examples:
>     *   `setEmulatorEnabled(enabled: boolean)`
>     *   `updateEmulatorMocks(mocks: MockMethodResponses)`
>     *   `triggerEmulatorEvent(eventDetails: EventDetails)`
> 
> Edit @google3/search/frontend/silk/tools/webinspector/injected/silk_web_inspector.ts:
> 3.  Import and instantiate `UnifiedPortsTransportSimulator`.
> 4.  Expose methods on `window.silkWebInspector` that the `WebPageCommunicationService` can call:
>     *   `setEmulatorEnabled`: Calls the simulator's `setEnabled` method and potentially `startSimulation` or `stopSimulation`.
>     *   `updateEmulatorMocks`: Calls the simulator's `updateMockMethodResponses` method.
>     *   `triggerEmulatorEvent`: Calls the simulator's `triggerEvent` method.
> 5.  Add `UnifiedPortsTransportSimulator` to the BUILD deps.

> [!NOTE]
> Implement Capabilities Header UI for Client Emulator.
> 
> Edit @google3/search/frontend/silk/tools/webinspector/app/panel.html:
> 1.  Within the "client_emulator_content" section, add:
>     *   A textarea for the base64 encoded `X-Silk-Capabilities` header string.
>     *   Informational text instructing the user to manually apply this header using a browser extension like ModHeader, as direct header manipulation is not possible.
> 
> Edit @google3/search/frontend/silk/tools/webinspector/app/panel.ts:
> 2.  No specific logic needed for header application, as it's a manual user step. The textarea is for user convenience.

---


Read PLAN.md thoroughly, and explore everything.

cl/901374424
Look at this CL. It is a proof of concept. Clean it up as best as you can, make it proper, maybe rewrite it fully, and add it to the Silk Web Inspector

cl/663317973
Look at this CL. It is a better proof of concept, which is fully finished and polished, but has less features I think(read descriptions of both and determine) and is weirdly done in the UI. It also is made for the 2024 version of the tool, from when it was simpler and we used multiple ports for silk.

Please try add client emulation to the silk web inspector, so that mock method responses or errors can be sent back to silk method calls, and mock events can be sent to the web.

Do not add any UI for now. In the future, we will have a section and two UIs to let users insert responses and errors, but for now I can test it by doing something like this https://docs.google.com/document/d/14UvT6u74P_IMtLQElzzK6T3qCa7V1NOCIhO38wKdqnc/edit?tab=t.0

---

Based on this unsubmitted cl/663317973 and [design doc](https://docs.google.com/document/d/1CPm-AD-hYdm4mUpUlF_4K-9iBbyd6fdDB1nl-LGlkDk/edit?resourcekey=0-LHB0Kt3Uv0bqWn4l3bgDQw&tab=t.0#heading=h.xzptrog8pyxf), which are outdated but are good and need updating, create some specific, fully realized prompts and an accompanying PLAN.md for the Silk Web Inspector, for following CLs:
- [Silk Web Inspector] Add client emulation to the tool for method calls (returning mock method responses and errors)
- [Silk Web Inspector] Add client emulation to the tool for events (pushed from tool to web)
- [Silk Web Inspector] Add client emulator tab to the tool
- [Silk Web Inspector] Add toggle for client emulation
- [Silk Web Inspector] Add fields for adding method responses and errors to the emulated client
- [Silk Web Inspector] Add fields for adding events to be pushed to web to the emulated client

Until the last CL, [these instructions](https://docs.google.com/document/d/14UvT6u74P_IMtLQElzzK6T3qCa7V1NOCIhO38wKdqnc/edit?tab=t.0#heading=h.me50u58ltqsu) should be able to be followed to test it.
Do not reuse the latest POC code, it is bad.

