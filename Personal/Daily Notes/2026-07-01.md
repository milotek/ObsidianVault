6am

- cl/925306603 send out comments
- cl/921982741 remove anal reviewer
- FUCK

  │ Yes — the follow-up CLs that consume this are:
  │
  │ • cl/933704588 — Introduces the two-port channel architecture. When  isClientConnected()  returns  true  (AGSA WebView), we interpose a fake  MessageChannel
  │ between the web page and the real native port so we can intercept and mock individual messages while forwarding unmatched ones to the real backend          
  │ (passthrough mode). When it returns  false  (Chrome DevTools), we create a fully synthetic port with no native backend (simulator mode).
  │ • cl/934059220 — Gates the entire mechanism behind a  enable_silk_client_emulator  user preference, so  isClientConnected()  is only evaluated when the     
  │ emulator is explicitly enabled.
  │
  │ The core reason for the distinction: in passthrough mode we need to capture the existing  silkPort  and set up bidirectional forwarding before the          
  │ instrumenter runs, whereas in simulator mode we create a port from scratch. The setup logic is fundamentally different for each path.