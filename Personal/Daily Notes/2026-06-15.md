So the client emulator is done, about to send it out for review after I break a couple things down into smaller CLs. It hopefully should be avaliable on webdevx this week.
I've also pre-emptively added the feature to inspect captured Silk messages with their field names visible, which should make debugging Silk much easier for developers.
Inspected messages can also have their information edited, copied and saved to be re-used with the client emulator, so users can catch real Silk messages and then










So imagine you are an AI mode developer and you want to test how AI mode reacts when you send a bunch of the same AI mode inputs. You could connect your real AGSA client and send an ai mode input, capture and save that 