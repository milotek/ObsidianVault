- `[Silk Web Inspector] Create client emulator class, mock store and insertion methods`
- `[Silk Web Inspector] Add "Silk Client Emulator" section to feature list in UI`
- `[Silk Web Inspector] Add fields for data entry for mocks in UI`
- `[Silk Web Inspector] Store data entry input into silkMocks`
- `[Silk Web Inspector] Display list of mocked methods and events to the user`
- `[Silk Web Inspector] Persist Silk mocks in WebDevX/Chrome extension storage`
- `[Silk Web Inspector] Add option for spoofing X-Silk-Capabilities header`
- `[Silk Web Inspector] Add client emulation to the tool for method calls (returning mock method responses and errors)`
- `[Silk Web Inspector] Add client emulation to the tool for events (pushed from tool to web)`
- `[Silk Web Inspector] Update go/silk-inspector documentation for Client Emulator`

Read [Silk Client Emulators Design Document](https://docs.google.com/document/d/1oI7YmXqOeO5fdOXRPBuO0VZ1rOPsVhlJzPftl4C5H5Q/edit?tab=t.0#heading=h.xzptrog8pyxf) thoroughly. We are going to implement this functionality into the Silk Web Inspector. 

- Define silkMocks in `types.ts` identically structured to silkPortsData. Define `MockResponseMessage` and `MockErrorMessage` types, identically to `ResponseMessage` and `ErrorMessage` respectively, but without the ID field.
- Alternatively, define silkMocks in `types.ts` identically structured to silkPortsData. Change `ResponseMessage` and `ErrorMessage` types so that ID is optional.
- Create a new class, `ClientEmulator`, which has privately scoped `silkMocks` property in it. It should have methods for inserting a silk method response (it should take string args, cast to a `ResponseMessage`, then store in silkMocks. There should also be a different method but same thing for method error). 
	- Add a function for returning a method or event if it exists in silkMocks. **btw 2 do this, we can look at the old code from the Silk Web Inspector (pre independent calls), where we used to match by ~~service~~ and method/event name.**
	- Add a function for returning
	- When instanciated, clientEmulator should load silkMocks from local storage, using the `chrome.storage.local` api. 
- In `unified_ports_transport_instrumenter.ts`
	- Check WebDevX preferences to see if client emulator has been enabled. If it has: Instantiate a clientEmulator.

- In `unified_ports_transport_instrumenter.ts`:
	- Modify the spyOnWebToNativeCalls to include a check like so:
		- If the method has a corresponding mock response/result (or error...) ()

---
- Per method and event, they each have at most 1 capability.
- That bubbles up into per service
- The methods and events you mock should be the truth for how the capabilities header is generated.
---
## Snippets

- Rewrote design