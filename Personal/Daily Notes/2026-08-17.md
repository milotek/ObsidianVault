Requested an exception for Rasta, finally, but I've been ramping up my 


---
## Web Platform
### View Transitions
- Watched the presentation from assema@.
- Interesting stuff.
- She asked me if I had any questions...
	- What if the screenshot is going to be resized to end up bigger, such as in the case of the image viewer? The pixels will be stretched and it will look weird, no?
	- ~~"Snapshots" = screenshots right?~~
	- So for everything they want to make appy they have an API on the server -i.e: `xgaUiTransitionService`, which pass throughs to the client doing things like screenshotting and hiding the native elements, the transition.
	- Because they've already made the transitions and effects for web, it'll show and look fine on xGA and mobile devices anyway, but it won't look "appy", so this layer allows them to do stuff like:
	- For gestures, you "invert" the snapshot model, so that the native app takes a screenshot of the web - I thought this is what we are doing for all of these? 
		- The web requests snapshots and position data for native elements to place them correctly.
		- The native client is signaled to hide its UI, allowing the web to seamlessly take over the animation.
		- Once the animation ends, a final signal restores the native elements and removes the snapshots.

```python
def openImageViewer():
	do_image_viewer_open()
	# ...
```

```python
if client == "xGA":
	do_appy_image_viewer_open()
	# ...
else:
	do_web_image_viewer_open()
	# ...
```

	- TLDR if they have the animation implemented, and they can coordinate it with the snapshots, then they can take advantage of whatever they built for web, and have a nicer transition on xGA.
	- For example: for overal modals, this is a matter of minutes as all they have to do is place the web snapshots on the correct Z layer, and then expand the web animaiton directly on top of it. 
- Race condition - is that to do with Silk latency?

---

- Block and re-issue does incur latency - Daniel is measuring this
- The work we're doing for enabling view transitions is removing the block-and-reissue behavior for within SRP clicks
- It looks the recent work related to this is mostly migrating some exisiting parameters to be GWS stick parameters - which means they get passed during the RPC request, and also migrating headers into the dynamic http header webview api
- ~~To ask~~:
	- What work has been done so far?
	- What is milotek@ going to be doing?
		- QueryLoader stuff and logic changes
		- eaporter@ has done soem work into moving some of that into a page plugin
			- `SuggestDataService` - cl/928630504 
		-  - b/539541206
 			- Look at `SuggestDataService` above as a reference as it has already been moved into a page plugin.
			- Plugins should survive longer, created at page init, maybe will run into problems with scope (UI?).
	- Will I be "taking over" from you or is this still parallelisable work?
		- The latter :D
- While drcastroa@ is working on [The dynamic headers API](https://docs.google.com/document/d/1gzOG8YTwSfk7L4FoLfDuhDjWWj422QWYZvZ7nqV4HB4/edit?resourcekey=0-SZf2MMgvCTdQxEbBhYjzMQ&tab=t.0#heading=h.c02q6xa5d0gp), we are doing preperatory work turning parameters into headers.
 - There's a decent amount of WebX ... 